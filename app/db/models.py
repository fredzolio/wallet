# Esse arquivo existe apenas para reunir todos os modelos em um único lugar
# para que o Alembic possa encontrá-los durante as migrações.
# Não é para ser importado por outros módulos.

from app.models.user import User
from app.models.document import Document
from app.models.recarga import Recarga
from app.models.transport import TransportCard