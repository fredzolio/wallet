# Changelog

{%- for version, release in ctx.history.released.items() %}
## {{ version.as_tag() }} ({{ release.tagged_date.strftime("%Y-%m-%d") }})

{%- if release.elements.get("breaking") %}
### ⚠ BREAKING CHANGES

{% for commit in release.elements.get("breaking", []) %}
* {% if commit.scope %}**{{ commit.scope }}:** {% endif %}{{ commit.descriptions[0] }} ([`{{ commit.hexsha[:7] }}`]({{ commit.hexsha | commit_hash_url }}))
{%- endfor %}
{% endif %}

{%- for type_, commits in release.elements.items() | dictsort %}
{%- if type_ != "unknown" and type_ != "breaking" and commits %}
### {{ type_ | title }}

{% for commit in commits %}
* {% if commit.scope %}**{{ commit.scope }}:** {% endif %}{{ commit.descriptions[0] }} ([`{{ commit.hexsha[:7] }}`]({{ commit.hexsha | commit_hash_url }}))
{%- endfor %}
{% endif %}
{%- endfor %}
{% endfor %} 